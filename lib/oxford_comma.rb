def oxford_comma(array)
  if array.length == 2 
    array.join (" and ")
  elsif array.length == 3
    final_string
    array.each do |index|
      if array.equal? array.last
        
        
  end
end